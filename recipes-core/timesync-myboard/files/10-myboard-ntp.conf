# The board has no RTC battery and no internet connection. It syncs its clock
# from the directly-connected build host (192.168.1.202) over the dev cable,
# which runs a local chrony NTP server. systemd-timesyncd is already enabled
# by the systemd preset; this drop-in only points it at the host.
[Time]
NTP=192.168.1.202
